# Shape Visuals

Initial repository setup for the Shape Visuals project.

This first commit includes the existing `venv/` directory and the repository
is configured to use the `main` branch with GitHub remote:
`https://github.com/Rabin935/Shape-visuals.git`
